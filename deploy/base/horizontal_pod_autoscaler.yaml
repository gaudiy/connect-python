apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: connect-python
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: connect-python
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
